# Decisions (ADR log, append-only)
# Format: [ADR-NNN] decision — why — rejected alternative (YYYY-MM-DD)
# Full design memo: C:\Users\Marcos\.claude\plans\quiero-que-trabajemos-juntos-pure-hammock.md
- [ADR-001] Locked stack: Next.js App Router + TS strict + Tailwind + Supabase + Biome + Vitest — harness default — (2026-07-01)
- [ADR-002] Performance-first POSITIONING, not literal "ignore longevity" — safety/fatigue/injury limits protect performance — rejected: peak-at-any-cost (self-contradictory) (2026-06-29)
- [ADR-003] Hybrid intelligence: sport-science scaffold + n-of-1 personalization + LABELED hypotheses; deterministic code computes, LLM only interprets — rejected: "discover causal laws" at n=2 (spurious correlations) (2026-06-29)
- [ADR-004] MVP = ONE narrow loop: training load + sleep + subjective readiness — cold-start/friction kills tracking apps — rejected: 18 domains day 1 (2026-06-29)
- [ADR-005] First MODULE of the all-in-one on a shared core (identity/timeline/synthesis/privacy) — forces core to be real — rejected: standalone (forks architecture) (2026-06-29)
- [ADR-006] Web (Next.js) as installable PWA — matches stack; Apple Health/Whoop via import later — rejected: native (HealthKit needs it, but friction/cost now) (2026-06-29)
- [ADR-007] Hybrid substrate: Observation spine + Metric registry + typed entities that EMIT observations; derived written back (source=derived); provenance first-class — rejected: generic-only (integrity loss) / typed-only (migration per metric) (2026-06-29)
- [ADR-008] Privacy: private silos + opt-in comparison; enforced at RLS AND AI context-assembly — rejected: fully shared / strict silos (2026-06-29)
- [ADR-009] MVP AI = proactive-minimal coach: grounded Q&A w/ cited sources + weekly synthesis w/ 1-2 labeled hypotheses; LLM built-in knowledge as scaffold — rejected: curated KB now (build cost before data) (2026-06-29)
- [ADR-010] Clean monolith, extractable core (core/ vs modules/fitness/) — no speculative abstraction, seams marked — rejected: monorepo now / pure standalone (2026-06-29)
- [ADR-011] Spine fine print: 3 provenance classes (raw editable; emitted/derived rebuildable, never edited directly); app-layer emission in-transaction (no DB triggers); client UUIDv7 + upsert (offline idempotency); 1 check-in/subject/local-date; canonical unit per metric; effective vs recorded time — (2026-06-30)
- [ADR-012] Engine: load = sRPE×duration (Foster); ACWR = EWMA only (7d/28d), bands 0.8-1.3/1.3-1.5/>1.5 as heuristic FLAGS never verdicts; 28d baselines, z<-1×2d flags; cold-start = progressive unlock w/ countdown; gap-robust (no-training=real 0, missing check-in=missing) — rejected: rolling 7:28 (slower, needs full window) (2026-06-30)
- [ADR-013] Capture: 1-screen tap-only check-in ~30s + immediate reward (state vs baseline); session ~20s w/ per-modality defaults; zero-guilt gaps; backfill YESTERDAY ONLY flagged backfilled=true — Marcos chose data honesty over continuity — rejected: 3-day window (2026-06-30)
- [ADR-014] Engine computes ON READ (pure TS over the spine); writing derived observations (source=derived) back to the spine is DEFERRED to the weekly synthesis job phase — at n=2 recomputing is free and avoids cache-invalidation complexity — rejected: materialize on every capture (2026-07-01)
- [ADR-015] Dashboards render charts as SERVER-SIDE SVG in RSC (zero deps, zero client JS; tooltips/interactivity deferred) — rejected: recharts/chart lib (client bundle + dependency for 2-3 static mobile charts) (2026-07-01)
- [ADR-016] AI at $0 (supersedes the paid-API assumption in ADR-009's mechanics, keeps its behavior contract): (A) in-app deterministic BRIEFING (copy button → any chat LLM) + (B) /coach skill in Claude Code (Marcos's subscription = frontier quality, $0 marginal): fetch raw data via Supabase MCP → compute briefing via script (deterministic, D2 invariant) → reason → INSERT labeled insights back so both users see them in-app. Provider-agnostic: the briefing is the stable interface; free-tier API (Gemini Flash) documented as future path if in-app automation is wanted (privacy caveat: health data through 3rd-party free tier). Privacy note: /coach runs with admin DB access — the D7 wall there is procedural (per-subject discipline in the skill) — rejected: paid API now (~$0.5-3/mo, Marcos wants $0), local LLM (quality/hardware) (2026-07-01)
