# State
Goal: MVP "Readiness Loop" v1 live + V2 build-out per roadmap. DoD gate: capture habit 4+ weeks (both users). Design memo: C:\Users\Marcos\.claude\plans\quiero-que-trabajemos-juntos-pure-hammock.md

Log:
- 2026-07-01/02 S1-S8: MVP v1 live (scaffold→substrate→capture→engine→dashboards→$0 coach, PWA/offline, Vercel prod deploy, ADR-001..021) + visual redesign ADR-019 + demo-subject seed ADR-020 + knowledge loop built (/harvest,/query,/lint-knowledge) + V2.0 interpretation layer SHIPPED (ADR-022, roadmap §A). Full episodes: journal.md.
- 2026-07-03 S9-S10: V2.1 OUTCOMES SHIPPED (ADR-023, 123 tests) — outcomes migration+RPC, per-modality emission, trends.outcomes 90d, Rendimiento UI, briefing Resultados, seed 324 obs; merged→main, prod smoke OK. Hydration error #418 on "/" found (pre-existing).
- 2026-07-03 S11: V2.2 PASSIVE INPUTS SHIPPED (ADR-024, 166 tests): device_connections+import_batches+import_observations upsert RPC, 5 recovery metrics, Apple HAE file import (/fuentes), Whoop OAuth env-gated "Próximamente" (real OAuth = whoop-device-arrival ritual, skills.md), trends/briefing/prefill/glossary surfacing; G-011 born; merged→main, prod deploy READY, smoke clean. Details: journal.md.
- 2026-07-07 S12: PLANNING-ONLY session. Apple sleep bug ROOT-CAUSED (hae.ts asleep:0 vs totalSleep). V2.3 PATTERN CANDIDATES plan APPROVED at plans\c-users-marcos-downloads-healthautoexpo-humming-parasol.md (binned+Spearman veto, /patrones 5th tab, 15-pair grid, lag-0, 56d unlock, compute-on-read, 84d seed w/ planted associations). Hydration #418 folded in as M0b.
- 2026-07-07 S13 (/execute, branch feat/v2.3-pattern-candidates): V2.3 PATTERN CANDIDATES SHIPPED end-to-end as ADR-025, all 6 milestones verify-green (159→198 tests); merged→main, pushed, prod deploy READY (dpl_GX34tVEDLsbhRMkwFxY9Gpsg79ye), smoke OK. M0: hae.ts prefers totalSleep over literal asleep:0 (real Watch S10 stage-based exports), stage-sum fallback, all-zero rows skipped, same-day sleep SUMS not averages. M0b: hydration #418 root-caused for real — Hero/Stat/ACWR label rows wrapped a `<p>` around InfoTip's popover `<div>` (invalid HTML nesting the browser silently un-nests); fixed to `<div>` wrappers, 0 console errors. M1: new engine/stats.ts (mean/pooledSd/cohensD/spearmanRho/alignLagged + sampleSd/aggregateByDate moved from load.ts/trends.ts) + engine/patterns.ts (computePatternCandidates: 15-pair grid, below/above-baseline or binary binning, Cohen's d + direction-normalized Spearman concordance veto, top-3 surfaced, formatCandidateEs); UnlockKey 'patterns' (56d). M2: service.ts getPatternCandidates (reuses cached fetch) + unlock boundary tests. M3: seed.ts extended 21d→84d (days 1-21 unchanged, 22-77 new "planted era" with ~12 LOW_SLEEP + ~10 ALCOHOL days carrying a real effect, caffeine a genuine negative control, 78-84 = original overreach block VERBATIM so all 3 V2.0 flags still fire); previewPatterns() dry-run mechanically asserts planted/negative-control/surfaced invariants; real seed run twice against the demo subject, 0 duplicate observation groups confirmed via SQL (idempotent). M4: /patrones page (5th tab) + PatternsSection.tsx (3 states, inline SVG "diff dumbbell", zero client JS) + glossary (patron/efecto/n); verified in-browser — 3 candidates render correctly with real seed data, popovers work, 5 tabs fit at 375px, 0 console errors (one stale-Turbopack-cache duplicate-declaration warning cleared on dev server restart, not a real bug — G-012). M5: briefing.ts gains a "Candidatos de patrón" section (same formatCandidateEs statement as the UI) + instruction rule 7 (cite exact effect+n, never cause); wired into /coach page + scripts/coach-briefing.ts + SKILL.md step 4; verified `pnpm briefing` against real demo data matches /patrones exactly. M6: full verify clean (198 tests), seed dry-run re-confirmed, real users' locked path confirmed via SQL (marcosnovella99@ historyDays=35, thomas 6 — both <56, correctly locked), Coach page in-browser confirmed to render the Candidatos section; ADR-025 + roadmap §B written.

Last done: V2.3 fully closed out — merged→main, deployed, Marcos re-uploaded his real 90-day HAE export at /fuentes and the M0 repair was SQL-confirmed (73 sleep_device rows, 0 zeros, range 0.3-10.25h; was 28/28 zero before the fix), knowledge loop run (/harvest: backfilled journal S12/S13, candidate C-014 filed, weekly-coach-run playbook updated), /lint-knowledge + janitor run to close the session clean (fixed the post-merge staleness above + a stale seed-demo-data playbook; deleted .next/ cache + 5 unused create-next-app template SVGs from public/, both janitor-flagged and Marcos-approved).

Next: nothing pending from V2.3 — it is fully shipped, deployed, and data-verified. Pick up the next roadmap item when Marcos is ready (V2.3 was the last item explicitly planned in §B; check roadmap.md for what's next, or await direction).

Deferred cleanup (low priority, from the janitor pass — Marcos wants it kept visible, not fixed now): `engine/baselines.ts`'s `trailingBaseline()` still computes mean/sample-SD inline instead of using the shared `mean()`/`sampleSd()` primitives in `engine/stats.ts` (which exist specifically to be the one source of this math, per the ADR-025 stats.ts extraction — this predates it). Consolidating is a judgment call, not urgent: `stats.ts` already does a type-only `import type { DatedValue } from './baselines'`, so a value-level import back from `baselines.ts` would need care to avoid a real circular import between the two modules.

Open questions: -

Standing (durable pointers): Supabase "athlete-os" id=zidgpigxtuzpnjvinidx (sa-east-1); G-000 HARD BAN on project "yogastasis"; 2 real users (marcosnovella99@, thomasnovella12@) + 1 e2e user + 1 demo user (demo@athleteos.app / DemoSeed2026!, disposable seed subject), siloed per D7; no service-role key stored anywhere. GitHub: MarcosNovella/athlete-os (public, main tracked). Vercel project prj_VC0NJaECdjP7UtPH96YDegesLkOe git-connected — push to main = auto prod deploy.
