# State
Goal: MVP "Readiness Loop" v1 live + V2 build-out per roadmap. DoD gate: capture habit 4+ weeks (both users). Design memo: C:\Users\Marcos\.claude\plans\quiero-que-trabajemos-juntos-pure-hammock.md

Log:
- 2026-07-01/02 S1-S8: MVP v1 live (scaffold→substrate→capture→engine→dashboards→$0 coach, PWA/offline, Vercel prod deploy, ADR-001..021) + visual redesign ADR-019 + demo-subject seed ADR-020 + knowledge loop built (/harvest,/query,/lint-knowledge) + V2.0 interpretation layer SHIPPED (ADR-022, roadmap §A). Full episodes: journal.md.
- 2026-07-03 S9-S10: V2.1 OUTCOMES SHIPPED (ADR-023, 123 tests) — outcomes migration+RPC, per-modality emission, trends.outcomes 90d, Rendimiento UI, briefing Resultados, seed 324 obs; merged→main, prod smoke OK. Found pre-existing hydration error #418 on "/" (TodayStatePanel InfoTip/Hero) — flagged, unfixed (still open, see below).
- 2026-07-03 S11: V2.2 PASSIVE INPUTS planned (plan approved at plans\vamos-a-planificar-la-lazy-meadow.md) then SHIPPED (ADR-024, 123→166 tests) on feat/passive-inputs via /execute, all 5 milestones verify-GREEN. M1 substrate: device_connections+import_batches (RLS-first) + import_observations upsert RPC + 5 recovery metrics; ENGINE_METRICS/coach SQL allowlist extended; seed gained two device batches through the real RPC (idempotency confirmed via SQL). M2 Apple HAE file import: tolerant Zod parser + Server Action upload + /fuentes 4th tab. M3 Whoop OAuth connector: full v2 integration (token refresh/rotation, wake-date attribution, conditional-claim sync), env-gated "Próximamente" (devices weeks away); first route handler in the repo (/api/whoop/callback). M4 surfacing: trends.ts recovery block, Tendencias "Recuperación" section, briefing section, check-in sleep prefill (editable), glossary (vfc/fc_reposo/recovery). M5 hardening: drove the real /fuentes upload flow end-to-end in-browser (synthetic HAE file via Blob/DataTransfer, no committed Playwright suite exists here), confirmed RPC convergence via SQL, re-seeded to restore the demo narrative; ADR-024 filed, roadmap marked shipped, whoop-device-arrival ritual in skills.md. Guardrail G-011 born ('use server' files may only export async — caught in-browser, not tsc/vitest). Knowledge loop run (harvest candidate C-013). Marcos approved merge ("dale hacelo" / "mergea") — ff-merged feat/passive-inputs→main, pushed both branches. Vercel deploy dpl_99yd2yv1cAzdBd3S6HMbzaK6Rjcd READY (target production, sha 4369903, alias athlete-os-pink.vercel.app). Prod smoke via claude-in-chrome on Marcos's real logged-in session: Hoy/Tendencias(Recuperación empty-state correct)/Coach/Fuentes(Whoop Próximamente, Apple upload ready) all render clean, 0 console errors across all 4 pages.

Last done: V2.2 passive inputs SHIPPED, merged to main, deployed to prod, smoke-tested clean on Marcos's real account.

Next: BUG reported by Marcos (2026-07-03, this session) — he uploaded a REAL Health Auto Export JSON from his Watch S10 at /fuentes; the upload succeeded but the Sueño (dispositivo)/sleep_device series does NOT show up correctly in Tendencias > Recuperación (recovery_score/hrv/resting_hr apparently fine, only the sleep read is off — Marcos didn't specify exact symptom, needs to be pinned down). FIRST STEP next session: get the actual uploaded JSON's sleep_analysis shape (ask Marcos to re-share the file, or query import_batches/observations for his subject to see what sleep_device value(s) actually landed) and compare against hae.ts's assumption (`asleep`/`totalSleep` numeric-hours fields) — real Health Auto Export sleep_analysis entries may use a different shape (e.g. separate inBed/asleep segments per night, or a `value`/`qty` field, or minutes/millis needing conversion) that the tolerant parser silently drops or miscomputes. Whoop real OAuth is a separate future ritual (whoop-device-arrival, skills.md) once devices physically arrive. Separately still pending: hydration error #418 on "/" (TodayStatePanel.tsx) — flagged three sessions running now, still unfixed, still out of scope each time — worth a dedicated small session.

Open questions: what exactly is wrong with the Apple sleep_device data in Tendencias — missing entirely, wrong value, wrong date, or chart not rendering? Marcos didn't specify; pin down before touching code.

Standing (durable pointers): Supabase "athlete-os" id=zidgpigxtuzpnjvinidx (sa-east-1); G-000 HARD BAN on project "yogastasis"; 2 real users (marcosnovella99@, thomasnovella12@) + 1 e2e user + 1 demo user (demo@athleteos.app / DemoSeed2026!, disposable seed subject), siloed per D7; no service-role key stored anywhere. GitHub: MarcosNovella/athlete-os (public, main tracked). Vercel project prj_VC0NJaECdjP7UtPH96YDegesLkOe git-connected — push to main = auto prod deploy.
